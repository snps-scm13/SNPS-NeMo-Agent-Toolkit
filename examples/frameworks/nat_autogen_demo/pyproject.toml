[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools >= 64", "setuptools-scm>=8"]

[tool.setuptools_scm]
git_describe_command = "git describe --long --first-parent"
root = "../../.."

[project]
name = "nat_autogen_demo"
dynamic = ["version"]
dependencies = [
  "nvidia-nat[autogen]~=1.3",
  "autogen-core~=0.7.0",
  "autogen-agentchat~=0.7.0",
  "autogen-ext~=0.7.0",
]
requires-python = ">=3.11,<3.14"
description = "AutoGen Multi-Agent Research Workflow Example"
keywords = ["ai", "rag", "agents", "autogen", "multi-agent"]
classifiers = ["Programming Language :: Python"]

[tool.uv.sources]
nvidia-nat = { path = "../../..", editable = true }

[project.entry-points.'nat.components']
nat_autogen_demo = "nat_autogen_demo.register"